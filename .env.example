# Environment Variables for Movie Recommendation System
# Copy this file to .env and fill in your actual API keys

# OMDb API Key for movie data (REQUIRED)
# Get your free API key from: https://www.omdbapi.com/apikey.aspx
OMDB_API_KEY=your_omdb_api_key_here

# Note: This app uses OMDb API for real-time movie data.
# The free OMDb plan provides 1,000 requests per day, which is perfect for testing.

# For local development:
# 1. Copy this file: cp .env.example .env
# 2. Get your OMDb API key from the link above
# 3. Replace 'your_omdb_api_key_here' with your actual key
# 4. Save the .env file

# Flask configuration (optional for local development)
FLASK_ENV=development
SECRET_KEY=your_local_secret_key_here
PORT=5000

# For Render deployment:
# Set these environment variables in your Render service dashboard:
# - OMDB_API_KEY (your actual OMDb API key)
# - FLASK_ENV=production  
# - SECRET_KEY (generate a random secret key)

# For Vercel deployment:
# Set OMDB_API_KEY as an environment variable in your Vercel dashboard